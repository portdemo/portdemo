'use strict';

var app = angular.module('portalApp');
app.controller('benefitsCtrl', ['$scope', function($scope){
	$scope.message = "Sorry, looks like something isn't working with the find benefits page. Please give us some time to fix it. Then, try again.";
}]);